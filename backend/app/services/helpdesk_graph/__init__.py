"""Phase-A helpdesk agent graph package.

The package starts with a synchronous supervisor skeleton and deterministic
tools, then grows into the full LangGraph checkpointer design in later phases.
"""
